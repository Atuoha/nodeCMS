'use strict';const mongoose=require("mongoose"),Schema=mongoose.Schema,categorySchema=new Schema({title:{type:String,minLength:5,required:!0},date:{type:Date}});module.exports=mongoose.model("categories",categorySchema);